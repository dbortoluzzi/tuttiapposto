<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.dbortoluzzi.data">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name = "it.dbortoluzzi.tuttiapposto.di.App"
        android:theme="@style/AppTheme">
        <activity android:name="it.dbortoluzzi.tuttiapposto.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="it.dbortoluzzi.tuttiapposto.ui.LoginActivity" />
        <activity android:name="it.dbortoluzzi.tuttiapposto.ui.RegisterActivity" />
    </application>

</manifest>